import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.royalschool.foropec',
  appName: 'ForoPEC',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
